target-version = "py310"
line-length = 88
extend-exclude = ["frontend"]

[lint]
select = ["E", "F", "I", "B", "UP", "ANN"]
ignore = [
  "ANN401", # allow `Any` when needed
]

[lint.per-file-ignores]
"tests/**" = ["ANN"]
"backend/api/**" = ["B008"]
